# Optional: override the backend base URL. Defaults to the same value the
# prototype used (the ASP.NET Core "https" launch profile).
# If you change this, also update the WithOrigins(...) list in the backend's
# Program.cs and, if needed, the port in vite.config.js.
VITE_API_BASE_URL=https://localhost:7167
